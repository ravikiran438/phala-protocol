\* TLC configuration for small-model verification of welfare-detector invariants.
SPECIFICATION Spec

CONSTANTS
    Agents = {a1, a2}
    DetectorTypes = {cognitive_load, autonomy, dignity}
    WeightKeys = {w1, w2}
    HashSpace = {h1, h2, h3}
    MaxHorizon = 4

INVARIANTS
    TypeOK
    TypedDetectorComposition
    ArbitrationDeterminism
    PredictiveHorizon
    DetectorProvenance
