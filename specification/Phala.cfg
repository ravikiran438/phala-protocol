\* TLC configuration for Phala.tla
\* Status: skeleton; Next/Spec are not yet filled in, so TLC cannot
\* currently run this module against a state space. The configuration
\* documents the intended constants for the first model-checking run.

CONSTANTS
    Agents = {a1, a2, a3}
    Tasks = {t1, t2}
    Contexts = {c1}
    MaxDepth = 3
    MaxVersions = 2
    WMin = -1
    WMax = 1

INVARIANTS
    Invariants

\* SPECIFICATION Spec    \* uncomment once Init / Next are defined
